{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@trade-pulse/web && cp -R apps/web/.next .next",
  "installCommand": "cd ../.. && pnpm install",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/generate-blog",
      "schedule": "0 8 * * *"
    }
  ]
}
