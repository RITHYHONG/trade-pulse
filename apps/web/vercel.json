{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@trade-pulse/web",
  "installCommand": "cd ../.. && pnpm install",
  "outputDirectory": "apps/web/.next",
  "crons": [
    {
      "path": "/api/cron/generate-blog",
      "schedule": "0 8 * * *"
    }
  ]
}
